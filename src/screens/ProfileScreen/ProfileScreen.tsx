import React, { PropsWithChildren } from "react";
import { View } from "react-native";
import { ProfileScreenProps } from "..";

const ProfileScreen: React.FC<PropsWithChildren<ProfileScreenProps>> = () => {
  return <View></View>;
};

export default ProfileScreen;
